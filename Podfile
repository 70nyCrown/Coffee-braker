# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Coffee braker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Coffee braker
  pod 'RxSwift', '6.5.0'
  pod 'RxCocoa', '6.5.0'
  pod 'FirebaseAuth'
  pod 'FirebaseFirestore'
  pod 'Alamofire'
  pod 'Realm'

  target 'Coffee brakerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Coffee brakerUITests' do
    # Pods for testing
  end

end
